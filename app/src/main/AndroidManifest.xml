<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="bd.gov.ansarvdpbank.avubinfo">

    <uses-permission android:name="android.permission.INTERNET" />

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Fudie"
        android:usesCleartextTraffic="true">
        <activity android:name=".RegionalOffices"></activity>
        <activity android:name=".DivisionActivity" />
        <activity android:name=".LoanActivity" />
        <activity android:name=".LoanDetailsActivity" />
        <activity android:name=".MdAddActivity" />
        <activity android:name=".ChairAddActivity" />
        <activity android:name=".BangabondhuActivity" />
        <activity android:name=".DmDirectorActivity" />
        <activity android:name=".MDirectorActivity" />
        <activity android:name=".ChairActivity" />
        <activity android:name=".ClientCornerActivity" />
        <activity android:name=".HotlineActivity" />
        <activity android:name=".ContactActivity" />
        <activity android:name=".QuestionActivity" />
        <activity android:name=".OfficeActivity" />
        <activity android:name=".DepositActivity" />
        <activity android:name=".MissionActivity" />
        <activity android:name=".BankIntro" />
        <activity android:name=".FullActivity" />
        <activity android:name=".ShareActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>